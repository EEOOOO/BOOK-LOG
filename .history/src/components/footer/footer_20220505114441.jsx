import React from 'react';
import styles from './footer.module.css';

const Footer = (props) => (
            <div className={styles.container}>
                <p className={styles.footerText}>Keep your BOOK HISTORY</p>
            </div>
    );

export default Footer;