import React from 'react';
import Header from '../header/header';

const Login = (props) => (
            <div className="container">
                <Header />
            </div>
    );

export default Login;