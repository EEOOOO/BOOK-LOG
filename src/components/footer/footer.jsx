import React from 'react';

const Footer = (props) => (
            <div className="container">
                <p className="footer_text">Keep your BOOK HISTORY</p>
            </div>
    );

export default Footer;